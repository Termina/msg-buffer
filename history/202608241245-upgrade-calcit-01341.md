# Upgrade to Calcit 0.13.41

- Upgraded the compiler and `@calcit/procs` runtime to the released 0.13.41.
- This release provides the explicit `&unit` value used by the UI event
  handlers, avoiding the legacy `;nil` compatibility form.
